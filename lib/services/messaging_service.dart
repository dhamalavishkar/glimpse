class MessagingService {
  Future<void> init() async {
    // Push notifications are temporarily disabled during Supabase migration.
    // To add them back later, consider integrating OneSignal or Supabase Edge Functions with FCM.
  }
}
